on:
  workflow_dispatch:
jobs:
  example:
    runs-on: ubuntu-latest
    steps:
    - name: Set API Key
      run: echo "API_KEY=12345" >> $GITHUB_ENV
    - name: Use APU key
      run: echo "Using API Key $API_KEY"
